version: '3'

services:
  webserver:
    image: pierrezemb/gostatic:latest
    restart: always
    ports:
    - "8080:8043"
    volumes:
    - /home/Peedy/Projects/tftp-ubuntu-installation/http:/srv/http